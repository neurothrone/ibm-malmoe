enum Destination {
  home("Home"),
  profile("Profile"),
  settings("Settings"),
  kaffson("Kaffson"),
  todaysLunch("Today's Lunch"),
  events("Events");

  const Destination(this.title);

  final String title;
}
